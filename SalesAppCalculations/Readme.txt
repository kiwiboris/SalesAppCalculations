Please make sure the App.config file has the following information:

1) Input File Path (in my example, Input1.txt);
2) Path to the Product Catalog file (in my example, ProductCatalog.xml).

The program will not run correctly unless the above are set.
